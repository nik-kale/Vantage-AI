import type { EventContext } from "../types";

export class NetworkCollector {
  // Placeholder for network event collection
  // Future: intercept fetch/XHR to track API failures

  start(callback: (ctx: EventContext) => void) {
    // TODO: Implement network monitoring
  }
}
