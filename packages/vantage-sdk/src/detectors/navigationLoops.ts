import type { EventContext, SuspicionSignal } from "../types";

export class NavigationLoopDetector {
  // Placeholder for navigation loop detection
  // Future: track repeated back/forward navigation patterns

  detectLoop(ctx: EventContext): SuspicionSignal | null {
    // TODO: Implement navigation loop detection
    return null;
  }
}
