import type { EventContext, SuspicionSignal } from "../types";

export class FormFailureDetector {
  // Placeholder for form failure detection logic
  // Future: track repeated submit attempts with validation errors

  detectFailure(ctx: EventContext): SuspicionSignal | null {
    // TODO: Implement form failure detection
    return null;
  }
}
